import { FormatPermissionPipe } from './format-permission.pipe';

describe('FormatPermissionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPermissionPipe();
    expect(pipe).toBeTruthy();
  });
});
